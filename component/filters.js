/*!
 *  all filters
 *  此文件用于所有过滤器
 */

/**
 * 获取用户信息
 * @param uid
 * @returns {*}
 */
export const toUpperCase = (value) => {
    if(value){
        return value.toUpperCase();
    }
    return value;
};

